{
  "name": "postcss-simple-url",
  "version": "0.1.6",
  "description": "PostCSS plugin to rebase urls from imports to make them work nicely with Webpack v2.",
  "keywords": [
    "css",
    "postcss",
    "postcss-plugin",
    "url",
    "rebase"
  ],
  "author": "Sebastian Werner",
  "license": "MIT",
  "repository": "https://github.com/sebastian-software/postcss-simple-url.git",
  "files": [
    "index.js"
  ],
  "dependencies": {
    "postcss": "^5.2.9"
  },
  "devDependencies": {
    "ava": "^0.17.0",
    "eslint": "^3.13.0",
    "postcss-smart-import": "^0.6.7"
  },
  "scripts": {
    "release": "release-it --github.release --npm.publish --non-interactive",
    "release:minor": "release-it --github.release --npm.publish --non-interactive --increment minor",
    "release:major": "release-it --github.release --npm.publish --non-interactive --increment major",
    "test": "ava"
  }
}
